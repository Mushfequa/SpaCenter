//- The navigation menu
//- this is manually updated, as it is more flexible
a(href='/', class=(currentUrl() == "/")? "active": "") Home
a(href='/users', class=(currentUrl() == "/users")? "active": "") Users

//-	TODO: use session to decide if we need to
//-	show login or logout here...
if(isAuthenticated())
	a(href='/logout') Logout #{username()}
else
	a(href='/login', class=(currentUrl() == "/login")? "active": "") Login
